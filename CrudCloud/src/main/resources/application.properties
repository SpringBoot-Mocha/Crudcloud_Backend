spring.application.name=CrudCloud

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always

# ============================================================================
# DATABASE CONFIGURATION - PostgreSQL (Deployed)
# ============================================================================
# Usa variables de entorno con fallback a valores por defecto
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://91.98.225.17:5432/CrudCloud}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Mocha2025Riwi+}
spring.datasource.driver-class-name=org.postgresql.Driver

# ============================================================================
# FLYWAY CONFIGURATION (Database Migrations)
# ============================================================================
# DISABLED: Flyway 11.1.0 doesn't support PostgreSQL 15.15
# Using Hibernate DDL instead (create tables automatically)
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.out-of-order=false

# ============================================================================
# JPA / HIBERNATE CONFIGURATION
# ============================================================================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# ============================================================================
# JWT CONFIGURATION
# ============================================================================
# Usa variable de entorno para JWT_SECRET (CRITICAL para producción)
jwt.secret=${JWT_SECRET:YW9lbmZ3b2VuZm93ZW5mb3dlbmZvd2VuZm93ZW5mb3dlbmZvd2VuZm93ZW5mb3dlbmZvd2VuZm93ZW5mb3dlbmZvd2VuZm93ZW5mb3dlbmZvd2VuZm93ZW5mb3dlbmZvd2VuZm93ZW5mb3dlbmZvd2VuZm93ZW4=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ============================================================================
# MERCADO PAGO CONFIGURATION
# ============================================================================
# IMPORTANTE: Obtener access token desde https://www.mercadopago.com/developers
mercadopago.access.token=${MERCADOPAGO_ACCESS_TOKEN:YOUR_MERCADOPAGO_ACCESS_TOKEN_HERE}
mercadopago.public.key=${MERCADOPAGO_PUBLIC_KEY:YOUR_MERCADOPAGO_PUBLIC_KEY_HERE}

# ============================================================================
# SSH CONFIGURATION (VPS Connection) - ALL FROM ENV VARS
# ============================================================================
ssh.host=${SSH_HOST:91.98.225.17}
ssh.port=${SSH_PORT:22}
ssh.username=${SSH_USERNAME:root}
# Use SSH key-based authentication (most secure)
ssh.private-key-path=${SSH_KEY_PATH:~/.ssh/id_rsa}
# Fallback to password authentication if key not available
ssh.password=${SSH_PASSWORD:javamochapm--3306}
ssh.connection-timeout=${SSH_CONNECTION_TIMEOUT:30000}
ssh.read-timeout=${SSH_READ_TIMEOUT:30000}

# ============================================================================
# DATABASE INSTANCE CONNECTION SETTINGS
# ============================================================================
# Host where instances will connect to (production IP)
database.host=91.98.225.17

# ============================================================================
# PROVISIONING SETTINGS
# ============================================================================
# PRODUCTION MODE: Provisioning permanently enabled
# All database instances will be created in Docker on VPS (91.98.225.17)
# SSH connectivity required for provisioning to work
provisioning.enabled=true

# ============================================================================
# EMAIL CONFIGURATION (Notifications) - ALL FROM ENV VARS
# ============================================================================
# Using Gmail SMTP (requires App Password, not regular password)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:crudcloudnotificaciones@gmail.com}
spring.mail.password=${MAIL_PASSWORD:omiaewghdaxhdgdl}
spring.mail.protocol=smtp
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.starttls.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.test-connection=false

# Thymeleaf Configuration for Email Templates
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# Email from address configuration
mail.from=${MAIL_FROM:noreply@crudcloud.com}
mail.from-name=${MAIL_FROM_NAME:CrudCloud}

# ============================================================================
# OAUTH CONFIGURATION (Google and GitHub)
# ============================================================================
# Google OAuth Configuration
# Client ID from: https://console.cloud.google.com/
# Project: CrudCloud (crudcloud-478604)
oauth.google.client-id=${GOOGLE_OAUTH_CLIENT_ID:60736221772-qu02faouq22nm1747c09ivtjb0m8298s.apps.googleusercontent.com}
oauth.google.client-secret=${GOOGLE_OAUTH_CLIENT_SECRET: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}

# GitHub OAuth Configuration
# Registered OAuth App: CrudCloud
# Created at: https://github.com/settings/developers
# Callback URL: http://localhost:3000/auth/github/callback (development)
# Registered by: JuanMendez98
oauth.github.client-id=${GITHUB_OAUTH_CLIENT_ID:Ov23litGnx91sQu0tdPN}
oauth.github.client-secret=${GITHUB_OAUTH_CLIENT_SECRET:ffe34ddef199de6998e27a0455a8afd1ecadede0}

# ============================================================================
# DISCORD WEBHOOK CONFIGURATION (Registration Notifications)
# ============================================================================
# Discord webhook URL for sending registration notifications
# Create a webhook in your Discord server at: Server Settings → Integrations → Webhooks
# Format: https://discord.com/api/webhooks/{webhook_id}/{webhook_token}
discord.webhook.url=${DISCORD_WEBHOOK_URL:}

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================
logging.level.root=INFO
logging.level.com.crudzaso=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.mail=DEBUG
